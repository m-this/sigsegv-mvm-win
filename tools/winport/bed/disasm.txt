# NextBotCreatePlayerBot<CTFBot> is inlined into all six callers. Its pieces,
# as the inlined copies call them:
# CTFBot::AllocatePlayerEntity, the function handed to the override
server 0x54f530 40
# ClientPutInServerOverride
server 0x271720 8
# after the cast: SetPlayerName(name), ClearFlags(), AddFlag(FL_CLIENT|FL_FAKECLIENT)
server 0x2e8cb0 30
server 0x1ff9e0 20
server 0x1ff3f0 30
# the tail of one inlined copy, from the cast to the return
server 0x594f40 60

# Turned-away candidates for the next functions the sweep reached.
# GetWeaponId(const char *): V_stricmp over the 0x6e weapon names
server 0x53ed20 30
# CBaseGrenade::GetThrower(): m_hThrower, then the original owner
server 0x20ad30 40
# Studio_BoneIndexByName(const CStudioHdr *, const char *), cdecl
server 0x21ca90 30
# CNavMesh::GetNavArea(const Vector &, float): who calls it on Linux, to find its Windows callers
server string CNavMesh::GetNearestNavArea
